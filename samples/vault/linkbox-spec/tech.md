---
tags:
  - spec
---

# Tech

- React + TypeScript
- Vite for build
- Tailwind for styling
- localStorage for storage (no backend)

All data lives in `localStorage` under the key `linkbox`. Works offline.
